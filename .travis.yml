language: node_js
node_js: stable

install:
  - npm install

script:
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config --global user.email "yqsy021@126.com"
  - git config --global user.name "yqsy"
  - git add .
  - git commd -m "Update docs"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master

env:
 global:
   - GH_REF: github.com/yqsy/yqsy.github.io
